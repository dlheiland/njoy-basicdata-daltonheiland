CREATE TABLE campaigns.message_transactional(
    id text PRIMARY KEY,
    clicks map<time,text>,
    delivery_status text,
    device text,
    message_revenue text,
    opens list<time>,
    profile_id text,
    send_time timestamp,
    template text
    );